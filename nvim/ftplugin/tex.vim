" Any tex file I'm editing is LaTeX.
let g:tex_flavor='latex'

" Faster (but inaccurate) syntax highlighting.
"let g:tex_fast=1

syntax spell toplevel

" Syntax highlight is so slow in TeX.
syntax off
